install

    git clone --recursive https://github.com/joshuarli/bin "${HOME}/bin"

build something

    ./pkg build ripgrep

todo

    ytop
    editorconfig-core-c
    rsync
    restic
      - waiting until 1.x
    mpv
      - i have a pkgbuild for this already, but may port over in the future

not necessary

    git (ubiquitous)
